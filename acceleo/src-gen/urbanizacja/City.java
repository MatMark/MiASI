package urbanizacja;

class City  extends Locality 
 {
// Atrybuty:
	public City	city_at;
	public Street	street_2;
// Getters & Setters:
	public City	getcity_at() { return city_at; }
	public void setcity_at(City w) { city_at = w; }
	public Street	getstreet_2() { return street_2; }
	public void setstreet_2(Street w) { street_2 = w; }
// Operacje:	
	public Boolean isBig () {
		//<> oznacza !=
		// Blad: Generic activity node! - preInit typu OpaqueAction
		x=new Integer(5);
		// Blad: Generic activity node! - Init typu OpaqueAction
		v=x;
		// Blad: Generic activity node! - if node typu DecisionNode
		invalid
		if (else) {
			// Blad: Generic activity node! - altern typu OpaqueAction
			z=56;
			// Blad: Generic activity node! -  typu MergeNode
			invalid
			// Blad: Generic activity node! - sw node typu DecisionNode
			invalid
			// Blad: Generic activity node! - exit typu OpaqueAction
			exit();
			// Blad: Generic activity node! -  typu MergeNode
			invalid
			if ('c') {
				// Blad: Generic activity node! - copy typu OpaqueAction
				memmove();
				// Blad: Generic activity node! -  typu MergeNode
				invalid
				
			}
			if ('e') {
				// Blad: Generic activity node! - erase typu OpaqueAction
				delete();
				// Blad: Generic activity node! -  typu MergeNode
				invalid
				
			}
			if ('d') {
				// Blad: Generic activity node! - display typu OpaqueAction
				display();
				// Blad: Generic activity node! -  typu MergeNode
				invalid
				
			}
			
		}
		if (<20) {
			// Blad: Generic activity node! - doSthElse typu OpaqueAction
			z=12;
			x--; //too big
			// Blad: Generic activity node! -  typu MergeNode
			invalid
			// Blad: Generic activity node! - sw node typu DecisionNode
			invalid
			// Blad: Generic activity node! - exit typu OpaqueAction
			exit();
			// Blad: Generic activity node! -  typu MergeNode
			invalid
			if ('c') {
				// Blad: Generic activity node! - copy typu OpaqueAction
				memmove();
				// Blad: Generic activity node! -  typu MergeNode
				invalid
				
			}
			if ('e') {
				// Blad: Generic activity node! - erase typu OpaqueAction
				delete();
				// Blad: Generic activity node! -  typu MergeNode
				invalid
				
			}
			if ('d') {
				// Blad: Generic activity node! - display typu OpaqueAction
				display();
				// Blad: Generic activity node! -  typu MergeNode
				invalid
				
			}
			
		}
		if (>3) {
			// Blad: Generic activity node! - doSth typu OpaqueAction
			v=sin(v)+x;
			return v;
			
			// Blad: Generic activity node! -  typu MergeNode
			
			invalid
			
			// Blad: Generic activity node! - sw node typu DecisionNode
			
			invalid
			
			// Blad: Generic activity node! - exit typu OpaqueAction
			
			exit();
			
			// Blad: Generic activity node! -  typu MergeNode
			
			invalid
			
			if ('c') {
			
				// Blad: Generic activity node! - copy typu OpaqueAction
			
				memmove();
			
				// Blad: Generic activity node! -  typu MergeNode
			
				invalid
			
				
			
			}
			
			if ('e') {
			
				// Blad: Generic activity node! - erase typu OpaqueAction
			
				delete();
			
				// Blad: Generic activity node! -  typu MergeNode
			
				invalid
			
				
			
			}
			
			if ('d') {
			
				// Blad: Generic activity node! - display typu OpaqueAction
			
				display();
			
				// Blad: Generic activity node! -  typu MergeNode
			
				invalid
			
				
			
			}
			
			
		}
		
		
		 }		
	public Integer getStreetsWithNumbers () {
			getStreetsWithNumbers();
			getStreetsWithNumbers isNumber isNumber_reply
			replyasynchCallsynchCall
			
				user : User [1] <<>>
				city <<>>
				street <<>>
		
		 }		
	
};
